
import 'package:flutter/material.dart';

class CrewGuardianMission extends StatelessWidget {
  const CrewGuardianMission({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Crew Guardian Mission"),
      ),
      body: const Center(
        child: Text("Crew Guardian Mission gameplay will be here."),
      ),
    );
  }
}
